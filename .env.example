# Optional — only needed if you use the ElevenLabs TTS provider.
ELEVENLABS_API_KEY=

# Optional — path to your local Rhubarb binary if it's not on $PATH.
# Default lookup: $PATH, /usr/local/bin/rhubarb, /opt/rhubarb/rhubarb, ~/bin/rhubarb
RHUBARB_BINARY=
